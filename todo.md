Do these patches one by one so we test and complete and save all changes robustly
[] there should be option to see which headers we are removing from UI view so that we have a seperated list of removed headers so that we can restore them anypoint in time and show them in UI again
